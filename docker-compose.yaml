version: "3.9"

services:
  controller:
    command: "tail -f /dev/null"
    stdin_open: true
    build: .
    volumes:
    - .:/app
    - /var/run/docker.sock:/var/run/docker.sock
    network_mode: host
