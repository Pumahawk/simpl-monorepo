[Unit]
Description=Tier1 env=local

[Service]
ExecStart=mise --cd "${SIMPL_PROJECT_HOME}" exec -- mvnd -f simpl-repo/simpl-cloud-gateway -Dmaven.test.skip spring-boot:run -Dspring-boot.run.profiles=local,participant
Environment=GATEWAY_URL=http://localhost:9010
StandardOutput=journal
StandardError=journal
